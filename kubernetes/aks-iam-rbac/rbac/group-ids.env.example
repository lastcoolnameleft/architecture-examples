# =============================================================================
# Entra ID Group Object IDs for Kustomize RBAC substitution
# =============================================================================
# Copy this file to rbac/cluster/group-ids.env and rbac/namespace/group-ids.env,
# then fill in the actual group object IDs, or run setup-demo-accounts.sh which
# writes both files automatically.
#
# NOTE: rbac/cluster/group-ids.env and rbac/namespace/group-ids.env are gitignored
#       (contain tenant-specific IDs). This example file is safe to commit.
# =============================================================================

INFRA_OPS_L2_GROUP_ID=<INFRA_OPS_L2_GROUP_OBJECT_ID>
PLATFORM_SRE_L3_GROUP_ID=<PLATFORM_SRE_L3_GROUP_OBJECT_ID>
APP_SUPPORT_L2_GROUP_ID=<APP_SUPPORT_L2_GROUP_OBJECT_ID>
COMMAND_CENTRE_L1_GROUP_ID=<COMMAND_CENTRE_L1_GROUP_OBJECT_ID>
CLOUD_DEPLOYMENT_GROUP_ID=<CLOUD_DEPLOYMENT_GROUP_OBJECT_ID>
CLUSTER_VIEWER_GROUP_ID=<CLUSTER_VIEWER_GROUP_OBJECT_ID>
