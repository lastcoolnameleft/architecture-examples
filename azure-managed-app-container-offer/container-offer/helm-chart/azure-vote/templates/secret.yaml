{{- if .Values.redis.password }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "azure-vote.fullname" . }}-redis-secret
  labels:
    {{- include "azure-vote.labels" . | nindent 4 }}
type: Opaque
data:
  redis-password: {{ .Values.redis.password | b64enc | quote }}
{{- end }}
